import { colors } from "../variables"
import Theme from "./Theme"

const light: Theme = {
    background: 'rgb(243, 244, 246)',
    body: colors.primary,
    colors,
    pages: {
        home: {
            background: '#fff',
        }
    }
}

export default light
